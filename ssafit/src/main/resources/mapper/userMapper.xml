<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.ssafit.model.dao.UserDao">

    <resultMap id="userResultMap" type="User">
        <id column="user_id" property="userId"/>
        <result column="password" property="password"/>
        <result column="user_name" property="userName"/>
        <result column="email" property="email"/>
        <result column="birth_date" property="birthDate"/>
    </resultMap>

    <select id="selectUser" parameterType="String" resultMap="userResultMap">
        SELECT * FROM user WHERE user_id = #{userId}

    </select>

    <select id="selectUserList" parameterType="HashMap" resultMap="userResultMap">
        SELECT * FROM user
        <!-- 이후 동적 쿼리 날리기 -->
        <if test="mode == 1">
            WHERE user_id LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="mode == 2">
            WHERE user_name LIKE '%${keyword}%'
        </if>

    </select>

    <insert id="insertUser" parameterType="User">
        INSERT INTO user(user_id, password, user_name,email, birth_date, sex)
        VALUES (#{userId}, #{password}, #{userName}, #{email}, #{birthDate}, #{sex});
    </insert>

    <update id="updateUser" parameterType="User">
        UPDATE user
        SET password = #{password}, user_name=#{userName}, birth_date=#{birthDate}, sex=#{sex}
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteUser" parameterType="String">
        DELETE FROM user
        WHERE user_id=#{userId}
    </delete>


</mapper>